# Comprehensive review — 2026-07-02

Five parallel review agents (Core, engine, UI, playability-vs-spec, tooling) over
master @ d049465 (350 Core tests green). This file is the live triage checklist for
the autonomous day: tick items as they land, note the commit.

## A. Code fixes (correctness)

Status: C1-C5 fixed in e2dc77e; E1-E4 + U1-U4 fixed in b753323 (U2 resolved as
option (b): deployment editing is uniformly live with JSON dedup; planner refuses
to open in non-plannable battles). In-game smoke pending with the next launch.

### Engine

- [x] **E1 HIGH** `PlanMissionLogic.ApplyPlan` (~:674) bypasses the plannability gate
  (`PlannableMission.CheckAfterStart` only enforced in `AfterStart`). Opening the
  planner in a battle the player doesn't command (campaign army member) and Applying
  hijacks the AI general's formations via `SetControlledByAI(false)` + executor
  orders; collides with RBM's `!IsPlayerGeneral && IsPlayerSergeant` tactics; seeds
  SessionPlanStore from a foreign battle. Fix: `_plannable` flag set in AfterStart;
  `ApplyPlan`/`SplitTroopsForPlan` no-op + log when false.
- [ ] **E2 MED** `HarnessRecorderLogic` recorder-fault path is dead: catch sets
  `_scenario = null` + `_forceEnd = true` but `OnMissionTick` early-returns on
  `_scenario == null`, so auto-leave/time-caps never run → armed run wedges at 9x
  forever on any recorder exception. Fix: check `_forceEnd` before the early return.
- [ ] **E3 MED** `HarnessRecorderLogic._clockSeconds` is documented wall-clock but
  accrues mission dt (9x under fast-forward) → hard-ends every armed run at ~180
  battle-seconds; `bySeconds: 240` scenarios get truncated and read as failures;
  paused game freezes the backstop. Fix: real `Stopwatch` started at
  OnDeploymentFinished; keep battle-time cap separate.
- [ ] **E4 LOW** `SubModule.OnSubModuleLoad`: `Harmony.PatchAll` unwrapped — a game
  update that removes the one patch target crashes at startup instead of degrading.
  Fix: try/catch + RbpLog + red toast (patch is camera-comfort only).

### UI

- [ ] **U1 HIGH** `PlanningModeVM.LiveSyncDuringDeployment` + `ExecuteApply` hand the
  draft's LIVE `BattlePlan` object to `ApplyPlan` (`PlanDraft.Build()` returns the
  backing plan; monitor keeps the reference) → after any live sync, every subsequent
  un-applied editor edit mutates the live plan in place, bypassing validation and
  monitor rebuild; Close-without-Apply doesn't discard. Fix: deep-copy at the seam
  (`PlanSerializer.DeepCopy`) in both paths.
- [ ] **U2 MED** LiveSync applies the ENTIRE draft on any map gesture, silently
  committing unrelated pending picker/list edits (breaks discard-on-close contract).
  DECISION (3 options): (a) keep partial-sync + deep copy, inconsistent contract;
  (b) make deployment-time editing uniformly live ("close = commit", validate each
  sync, update hint text) — also playability item 11; (c) scope sync to the map
  delta only (complex). **Chosen: (b)** — uniform, intuitive, matches the 30-60s
  goal; validation still gates each sync.
- [ ] **U3 LOW** `EditRailRow` dead click on ragged multi-select rail rows (row shows
  a real stage from a longer selection; click targets first selected formation,
  StageAt null, no-op). Fix: pick first selected formation with a stage at index.
- [ ] **U4 LOW** `ExecuteApply` closes+discards the session even when `ApplyPlan`
  threw (exception swallowed in ApplyEditedPlan). Fix: apply callback returns bool;
  only close on success; surface failure in StatusText.

### Core (all unit-testable)

- [ ] **C1 MED** `PlanMonitor` resume scan reads EnemyCommits sustain state frozen at
  suspension ("currently holds" can be a minute-old 1s approach) → resume enters
  Charge vs a long-stationary enemy. Fix: purge `_sustainedSince`/`_approach`
  entries for a formation's conditions on Suspend. + test.
- [ ] **C2 MED** StageCompleted emitted for stages that were just SKIPPED (steering
  invalidation path emits Skipped(i) then Activate emits Completed(i); hold-exit
  emits Completed for a never-executed held index) → contradictory event stream,
  ProgressionService awards 0.5+2.5 XP for a failed stage. Fix: track "genuinely
  executing", suppress Completed for skipped/held. + tests.
- [ ] **C3 MED** Final executed stage never emits StageCompleted (only fires on
  advancing off a stage) → single-stage plans earn 0 familiarity XP forever. Fix:
  `PlanMonitor.CompleteActiveStages()` at battle end (Active, non-holding), called
  from the OnBattleConcluded call site. + tests.
- [ ] **C4 MED** Opening stage with explicit `[BattleStart]` trigger rolls fidelity
  reaction-delay/drift, unlike the identical implicit (empty-When) form. Fix:
  `isOpeningPosture` treats all-BattleStart as empty (mirrors SelectResumeStage).
  + test.
- [ ] **C5 LOW** RunRecorder records MoveTargetChanged as `WaypointReached` with the
  NEXT waypoint number → AAR says "reached waypoint 2" when wp1 was reached; final
  waypoint never gets a line. Fix: record the waypoint just reached; MoveTo-complete
  already covers the last one (verify wording). + test update.

## B. Tooling fixes + dev-loop upgrades

Status: T1-T8 + T11 shipped in efa3457 (mdk.ps1; respawn/map-iterate/run-harness
fixed; rbp.plan_load; autobattle guard; last-battle.record.json + aar.txt +
battle_result.json; docs/preset sweep). T9 (dbg.battle self-wait) and T10
(one-command iterate) deferred — respawn.ps1's retry loop covers most of both.

- [ ] **T1 HIGH** `DebugPlanLoader.Enabled` unreachable (nothing sets it);
  `rbp-gameplay-check.json` claims a governed battle but runs ungoverned →
  misleading telemetry. Fix via **T7** (`rbp.plan_load`) and make the script honest.
- [ ] **T2 HIGH** `respawn.ps1` Wait-Line matches the FAILED dbg.battle refusal line
  (ok:false contains 'dbg.battle'), tail-1 races hide awaited lines, final wait
  polls a stale tail. Fix with **T6** (mdk.ps1 helper): parse JSON, check ok, retry
  on view-layer error.
- [ ] **T3 MED** `map-iterate.ps1` can convert a STALE BMP (same name reused; only
  waits for existence) → agent Reads an outdated UI screenshot. Fix: delete BMP
  before `shot`, wait on fresh LastWriteTime; guard null $img after retries.
- [ ] **T4 MED** `run-harness.ps1` can't dismiss the BLSE safe-mode dialog after its
  own force-kill (dev-relaunch has the Win32 dismiss; run-harness doesn't). Fix:
  reuse `DevWin::DismissSafeMode` or delegate launch to dev-relaunch.
- [ ] **T5 LOW** `rbp.autobattle` lacks the cold-launch guard dbg.battle has
  (ThumbnailCacheManager.Current null → NRE wedge via dbg.exec). Duplicate the gate.
- [ ] **T6** `tools/mdk.ps1 <command...>` send-and-await channel helper (parse new
  out.jsonl lines only, return parsed object, non-zero on ok:false). Root-cause fix
  for T2-class bugs; every loop gets shorter and correct.
- [ ] **T7** `rbp.plan_load <file>` console cmd: validate plan JSON → SessionPlanStore
  for next battle. Enables "edit JSON → dbg.restart" plan iteration; fixes T1.
- [ ] **T8** Structured outcome files: RBP writes `Logs\aar.json` at battle end; MDK
  writes `Debug\battle_result.json` (result, duration, per-formation casualties).
  One Read replaces log parsing.
- [ ] **T9** `dbg.battle` self-wait on cold launch (queue preset until ViewLayerReady
  via the TickRestart pattern) — kills the 18s magic sleeps everywhere.
- [ ] **T10** One-command iterate: `respawn.ps1 -Scenario <n>` chains relaunch →
  harness_arm → dbg.battle → dbg.ready → wait record → harness_diff.
- [ ] **T11** Docs: SKILL.md camp.load-guard note stale; mod-debug-kit-io.md missing
  dbg.track/track.jsonl; AGENTS.md "harness does not spawn battles" stale; sweep
  live-only presets (m12.json) into the repo.

## C. Playability backlog (ordered; full detail in the agent report, key items)

1. [x] On-screen "Battle Plan" button during deployment — RbpHudView entry chip,
   VERIFIED on screen.
2. [x] Signal-palette legend pills mid-battle — verified incl. ✓ latch on fire.
3. [x] Resume without console — Numpad5 key + HUD "Resume plan" chip (Numpad0
   stays the planner toggle in all modes); suspend bark updated.
4. [x] AAR popup at battle end (ShowInquiry at OnMissionResultReady) — code in;
   on-screen verify pending a resolved battle.
5. [x] Pattern inserts — PATTERNS footer row → picker (hold+charge-on-signal /
   skirmish+withdraw), applies to map selection; drivable via planner.cmd
   patterns/pick. Verify pending relaunch.
6. [ ] Fidelity/progression ON by default in campaigns.
7. [x] Tier-up toasts + commander growth lines in the AAR popup
   (ProgressionService.FamiliarityXpOf/TierOf; campaign-only visible).
8. [x] Carry hygiene — PlanRemapper.StripSceneAnchors wired into AfterStart
   (Core-tested); relative-plan carry verified in-game.
9. [x] "All" selection chip on the map (toggle) — visible; functional verify via
   planner.cmd selectall pending relaunch.
10. [ ] March-path polylines on the map.
11. [ ] Escape closes planner.
12. [ ] First-open guided hint card.
13. [ ] B7 per-formation HUD column.
14. [ ] Named plan presets on disk.
15. [x] Field-planning gesture hint (one message per battle when the cursor
    first crosses the boundary with a selection).
16. [ ] Rebindable keys via config file.
17. [ ] Commander name + tier pip on map blocks.
18. [ ] One-click Feigned Retreat insert.
19. [ ] Single-level undo.
20. [x] Enemy strength "~N" labels on the map — verified on screen.

Extra shipped: harness fast-forward actually holds now (engine clears
IsFastForward at TakeControlOfAgent; re-asserted per tick) — armed packs run ~9x.
planner.cmd gained selectall/patterns/pick <i> so every picker flow is
file-drivable.

## Execution order for the day

1. Core fixes C1-C5 (dotnet test) → commit per fix.
2. Engine fixes E1-E4 + UI fixes U1-U4 (compile + one relaunch smoke) → commits.
3. Tooling T1-T8 (T9-T10 as needed) — pays for itself for the rest of the day.
4. Playability iterations in backlog order, game up, verify each via file channel
   + screenshots; commit each.
